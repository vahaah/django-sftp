============
Installation
============

At the command line::

    $ easy_install django-sftp

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-sftp
    $ pip install django-sftp
